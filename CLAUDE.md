# Voxel Society Simulator

This project's agent-behavior policy lives in @AGENTS.md, not here — read it before starting
any task in this repo. This file exists only so Claude Code's automatic CLAUDE.md load picks it
up; AGENTS.md stays the single source of truth (kept tool-agnostic for Codex/Aider/etc., which
follow the AGENTS.md convention directly).
